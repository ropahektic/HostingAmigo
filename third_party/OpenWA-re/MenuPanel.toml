[[function]]
va = 0x00540780
name = "MenuPanel__CenterCursorOnFirstKindZero"
plate_comment = "MenuPanel::CenterCursorOnFirstKindZero (0x00540780) — usercall(ESI=panel), no stack args, plain RET. Walks the panel.items array and, for any item whose kind == 0, sets panel.cursor_x/_y to the center of that item's clip rect and panel.cursor_active = 1. Used by OpenEscMenuConfirmDialog to default-park the cursor on the \"Yes\" button (kind=0) when the confirm dialog opens. Multiple items with kind==0 → last one wins (overwrites)."

[[function]]
va = 0x005407D0
name = "MenuPanel__SetCursorAt"

[[function]]
va = 0x00540810
name = "MenuPanel__ActivateAtCursor"

[[function]]
va = 0x005408B0
name = "MenuPanel__HitTestCursor"

[[function]]
va = 0x00540B00
name = "MenuPanel__Render"
calling_convention = "__stdcall"
custom_storage = true
plate_comment = """
MenuPanel::Render — per-frame incremental redraw of the panel canvas.

usercall: EDI = MenuPanel* this. Returns panel.display_a (DisplayBitGrid* — the canvas the panel was painted into) so the caller can pass it to display.draw_scaled_sprite.

Phase 1: For each item, set items[i].is_highlighted_this_frame (+0x2C) = 0.

Phase 2: If panel.cursor_active != 0:
  - If panel.slider_lock != 0: that index's flag is set to 1 (sticky drag).
  - Else: hit-test cursor against each item.clip_left/_top/_right/_bottom; if a hit, that item's flag is set to 1.

Phases 3+4: Two passes over items checking
  items[i].is_highlighted_persistent (+0x28) != is_highlighted_this_frame (+0x2C).
For each item where the state changed:
  - Pick color: color_a (0x20) if turning OFF, color_b (0x24) if turning ON.
  - Redraw item label via display_b.draw_text_on_bitmap (slot 7) onto display_a.
  - Redraw 4 highlight-border lines (display_a vtable slots 1/2 = fill_hline/fill_vline of BitGridDisplayVtable).
  - Optionally call FUN_004F66E0 (?) for slider tick marks when item's render_aux is set.
End of phase 4: items[i].is_highlighted_persistent = is_highlighted_this_frame (commit).

This means the canvas is built once and then dirty-redrawn each frame to track hover state — no need to repaint the whole panel every frame.

IMPORTANT: items[i] fields at +0x28 and +0x2C were previously named neighbor_prev/neighbor_next on the assumption they were navigation links. They are NOT — they are transient render state used only by this function. Rename to `is_highlighted_persistent` / `is_highlighted_this_frame`.
"""

  [function.signature]
  returns = "DisplayBitGrid *"

  [[function.param]]
  name = "panel"
  type = "MenuPanel *"
  storage = "EDI"

[[struct]]
name = "MenuItem"
namespace = "/OpenWA"
size = 0x38
rust_path = "openwa_game::engine::menu_panel::MenuItem"
plate_comment = "One row in MenuPanel.items. Stride 0x38. Populated by MenuPanel::AppendItem (0x005408F0)."

  [[struct.field]]
  offset = 0x00
  name = "kind"
  type = "int"
  comment = "Icon / kind code (0..3 buttons, 4 for the volume slider)."

  [[struct.field]]
  offset = 0x04
  name = "label"
  type = "char *"

  [[struct.field]]
  offset = 0x08
  name = "x"
  type = "int"

  [[struct.field]]
  offset = 0x0C
  name = "y"
  type = "int"

  [[struct.field]]
  offset = 0x10
  name = "clip_left"
  type = "int"

  [[struct.field]]
  offset = 0x14
  name = "clip_top"
  type = "int"

  [[struct.field]]
  offset = 0x18
  name = "clip_right"
  type = "int"

  [[struct.field]]
  offset = 0x1C
  name = "clip_bottom"
  type = "int"

  [[struct.field]]
  offset = 0x20
  name = "color_a"
  type = "int"
  comment = "Primary color (0x10 default)."

  [[struct.field]]
  offset = 0x24
  name = "color_b"
  type = "int"
  comment = "Secondary color (0x0F default)."

  [[struct.field]]
  offset = 0x28
  name = "was_highlighted"
  type = "int"
  comment = "Committed highlight state currently drawn on the canvas. -1 = dirty/never drawn (forces repaint next frame), 0 = drawn as un-highlighted, 1 = drawn as highlighted."

  [[struct.field]]
  offset = 0x2C
  name = "is_highlighted"
  type = "int"
  comment = "This-frame hit-test result. Compared against was_highlighted in MenuPanel::Render to decide which items to repaint."

  [[struct.field]]
  offset = 0x30
  name = "render_ctx"
  type = "void *"
  comment = "Slider: pointer to Fixed* value. Minimize-style buttons: world.display. Plain buttons: null."

  [[struct.field]]
  offset = 0x34
  name = "render_aux"
  type = "void *"
  comment = "Set only when render_ctx is non-null."

[[struct]]
name = "MenuPanel"
namespace = "/OpenWA"
size = 0x3D4
rust_path = "openwa_game::engine::menu_panel::MenuPanel"
plate_comment = "0x3D4-byte UI widget. Allocated by create_camera_object; used as both the in-game camera (runtime.menu_panel_a/_b) and the ESC menu (items array)."

  [[struct.field]]
  offset = 0x00
  name = "display_a"
  type = "DisplayBitGrid *"
  comment = "Primary render target — the BitGrid canvas the panel paints into."

  [[struct.field]]
  offset = 0x04
  name = "display_b"
  type = "DisplayGfx *"
  comment = "world.display — used for text measurement (slot 10) and label rasterization (slot 7)."

  [[struct.field]]
  offset = 0x08
  name = "color_low"
  type = "int"

  [[struct.field]]
  offset = 0x0C
  name = "color_high"
  type = "int"

  [[struct.field]]
  offset = 0x10
  name = "cursor_x"
  type = "int"

  [[struct.field]]
  offset = 0x14
  name = "cursor_y"
  type = "int"

  [[struct.field]]
  offset = 0x18
  name = "cursor_active"
  type = "int"
  comment = "Set by SetCursorAt; cleared by OpenEscMenu/ConfirmDialog reset. Gates the hover-highlight box."

  [[struct.field]]
  offset = 0x1C
  name = "clip_left"
  type = "int"

  [[struct.field]]
  offset = 0x20
  name = "clip_top"
  type = "int"

  [[struct.field]]
  offset = 0x24
  name = "clip_right"
  type = "int"

  [[struct.field]]
  offset = 0x28
  name = "clip_bottom"
  type = "int"

  [[struct.field]]
  offset = 0x2C
  name = "slider_lock"
  type = "int"
  comment = "When non-zero, holds the index of the slider item currently being dragged."

  [[struct.field]]
  offset = 0x30
  name = "items"
  type = "MenuItem[16]"

  [[struct.field]]
  offset = 0x3B0
  name = "item_count"
  type = "int"

  [[struct.field]]
  offset = 0x3B4
  type = "byte[0x20]"
