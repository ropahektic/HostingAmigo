[[function]]
va = 0x00569BF0
name = "FramePostProcessHook__Destructor"
calling_convention = "__thiscall"
custom_storage = true
plate_comment = """
FramePostProcessHook::Destructor — base destructor for the abstract class. Rebinds vtable to PTR_FUN_0066a2b8 (the abstract base vtable, where slots 1 and 2 are _purecall) and frees the object if `flags & 1`. Concrete subclasses (e.g. ScreenshotHook with vtable 0x66A2C4) inherit this destructor unchanged.

The class itself is 4 bytes (just a vtable pointer). Instances live in DisplayGfx's std::vector at +0x24DF8 and are dispatched once per frame via DisplayGfx::DispatchFramePostProcessHooks (0x56CDB0).
"""

  [function.signature]
  returns = "FramePostProcessHook *"

  [[function.param]]
  name = "this"
  type = "FramePostProcessHook *"
  storage = "ECX"

  [[function.param]]
  name = "flags"
  type = "uint"
  storage = "stack:0x4"

[[struct]]
name = "FramePostProcessHook"
namespace = "/OpenWA"
size = 0x4
rust_path = "openwa_game::render::display::frame_hook::FramePostProcessHook"
plate_comment = """
FramePostProcessHook — abstract polymorphic class (just a vtable ptr).
4 bytes. Concrete subclass (ScreenshotHook, vtable 0x66A2C4) shares the
same layout — all state lives in globals.
"""

  [[struct.field]]
  offset = 0x0
  name = "vtable"
  type = "FramePostProcessHookVtable *"
  size = 0x4

[[vtable]]
name = "FramePostProcessHookVtable"
va = 0x0066A2B8
size = 3
plate_comment = """
Vtable for FramePostProcessHook (3 slots, 0x66A2B8). Slots 1 and 2 are
_purecall on the abstract base — only the concrete ScreenshotHook
(0x66A2C4) provides real implementations. The `wa_function` links point
at the ScreenshotHook concrete impls so the slot fn-pointer types
inherit from there; `this` is typed as `FramePostProcessHook *` to
match the polymorphic dispatch.
"""

  [[vtable.slot]]
  index = 0
  name = "destructor"
  wa_function = "FramePostProcessHook__Destructor"
  doc = "Scalar deleting destructor. Calls return value is `*mut FramePostProcessHook` per the MSVC ABI."

  [[vtable.slot]]
  index = 1
  name = "get_capture_request"
  wa_function = "ScreenshotHook__GetCaptureRequest"
  doc = "Returns non-zero if this hook wants to run this frame. ScreenshotHook returns 2 when a screenshot is pending."

  [[vtable.slot]]
  index = 2
  name = "capture"
  wa_function = "ScreenshotHook__CaptureToPng"
  doc = "Runs the hook's per-frame work, given the rendered layer_0 DisplayBitGrid from DisplayGfx+0x3D9C."
